engine-strict=true

# This is a comment

# Initialize a variable to store the sum
sum = 0

# Loop from 1 to 100
for i in range(1, 101):
  # Add the current number to the sum
  sum = sum + i

# Print the sum
print(sum)
